export type ContactInformationType = {
  icon: string;
  text: string;
  href: string;
};

export type CourseType = {
  name: string;
  instructor: string;
  date: Date;
};

export type ConferenceType = {
  name: string;
  date: Date;
};
