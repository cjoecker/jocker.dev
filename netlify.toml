[build]
  command = "react-router build"
  publish = "build/client"

[dev]
  command = "react-router dev"

[functions]
  #necessay for sentry together
  included_files = ["instrument.server.mjs"]
[functions.environment]
  NODE_OPTIONS = "--import ./instrument.server.mjs"
